export default definePageConfig({
  navigationBarTitleText: '技能帖详情',
  navigationBarBackgroundColor: '#FF6B9D',
  navigationBarTextStyle: 'white',
  backgroundColor: '#f8f9fa'
})

function definePageConfig(config: any) {
  return config
}
